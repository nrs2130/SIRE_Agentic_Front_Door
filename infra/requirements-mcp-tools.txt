# Slim runtime deps for the L4 MCP tool servers container (infra/mcp-tools.Dockerfile).
# Mirror the exact pins in the root requirements.txt; tool servers need nothing heavier.
mcp[cli]==1.28.1,<2
python-dotenv>=1.0.0
httpx>=0.27.0
opentelemetry-api==1.43.0
opentelemetry-sdk==1.43.0
